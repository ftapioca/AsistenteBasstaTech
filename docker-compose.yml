services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: file:/app/data/dev.db
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
